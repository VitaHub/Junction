<div class="header">
	<% if user_signed_in? %>
		<p class="pull-right"><%= link_to 'Log Out', destroy_user_session_path, :method => :delete %><%= link_to 'Edit profile', edit_user_registration_path %></p>
		<p><%= current_user.email %></p>
		
	<% else %>
		<span class="pull-right"><%= link_to 'Log In', new_user_session_path %> <%= link_to 'Sign Up', new_user_registration_path %></span>
	<% end %>
</div>